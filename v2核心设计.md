## 一、设计目标

1. **主程序只提供框架**：托盘、定时器、热更新、开机启动、UI导航等通用能力。
2. **插件全权负责协议**：登录/登出/在线检测，以及自己的配置界面、网卡绑定策略。
3. **配置实时保存**：插件直接修改自己的配置并写入文件，主程序不干预。
4. **树形导航界面**：左侧树节点（通用设置 / 插件 / 插件名称），右侧动态显示对应的配置面板。

---

## 二、架构分层

| 层级         | 职责                                              | 示例                    |
| ---------- | ----------------------------------------------- | --------------------- |
| **主程序**    | 托盘图标、定时调度、日志显示、插件加载、树形UI、全局设置（自动重连、定期登录间隔、开机自启） | `NetworkLoginApp`     |
| **插件基类**   | 定义插件必须实现的接口：名称、描述、创建配置控件、创建协议处理器                | `ProtocolPlugin`      |
| **协议处理器**  | 实现实际的 `check_online()` 和 `login()` 逻辑           | `PortalHandler`       |
| **插件配置控件** | 提供 `QWidget`，实时修改插件配置字典并直接写文件                   | `PortalConfigWidget`  |
| **配置文件**   | YAML格式，包含全局设置、当前激活插件、各插件的配置段                    | `network_config.yaml` |

---

## 三、关键接口

### 1. 插件基类 (`plugin_base.py`)

```python
class ProtocolPlugin(ABC):
    name: str
    description: str
    def create_config_widget(plugin_config: dict, config_path: str) -> QWidget
    def create_handler(global_settings: dict, plugin_config: dict, bind_ip: str) -> ProtocolHandler
```

### 2. 协议处理器接口

```python
class ProtocolHandler(ABC):
    def check_online(self) -> bool
    def login(self) -> (bool, str)
    def logout(self) -> (bool, str)  # 可选
```

### 3. 插件配置控件要求

- 接收 `plugin_config` 字典（可变引用）和配置文件路径。
- 通过信号/槽监听控件变化，防抖后直接更新 `plugin_config` 并写回配置文件。
- 无需主程序调用保存方法。

---

## 四、主程序改造要点

1. **UI布局**：`QSplitter` + 左侧 `QTreeWidget` + 右侧 `QStackedWidget`
   
   - 树结构：  
     
     ```
     ├─ 通用设置
     │  └─ 基本设置
     └─ 插件
        ├─ 插件A
        ├─ 插件B
        └─ ...
     ```
   
   - 通用设置页面内放置：自动重连、定期登录开关及间隔、开机自启动、当前使用协议下拉框等。

2. **插件加载**：扫描 `plugins/` 目录，动态加载所有 `ProtocolPlugin` 子类，实例化后存入列表。

3. **配置管理**：
   
   - 全局设置存于 `Settings` 节。
   - 各插件配置存于 `plugin_configs[插件名]` 节。
   - 主程序启动时，若旧配置无插件段，自动迁移到默认内置插件。

4. **协议切换**：
   
   - 通用设置中提供下拉框选择当前生效的插件。
   - 用户切换后，主程序销毁旧的 `ProtocolHandler`，使用新插件创建 handler，并重启网络监控和定期登录定时器。

5. **移除旧逻辑**：删除硬编码的 `LoginWorker`、`get_network_interfaces` 等（工具函数移至 `utils.py` 供插件使用）。

---

## 五、插件开发规范

- 每个插件是一个 `.py` 文件，放在 `plugins/` 目录。
- 必须导出类名为 `Plugin` 的 `ProtocolPlugin` 子类。
- 可以任意使用主程序提供的工具函数（如 `send_http_via_socket`、`check_network_connectivity`）。
- 配置控件内可任意设计界面（包括网卡选择、字段编辑、高级选项等），只需保证修改后实时保存到文件。
- 插件应尽量无外部依赖，或自行处理 `ImportError` 给出友好提示。

---

## 六、工作流示例

1. 用户启动程序 → 主程序加载配置、扫描插件、构建树形UI。
2. 用户点击“插件 → 通用Portal协议” → 右侧显示该插件的配置界面（包含网卡选择、账号密码等）。
3. 用户修改账号 → 插件控件防抖后直接写入 `network_config.yaml`。
4. 用户在通用设置中选择“当前使用协议”为“通用Portal协议” → 主程序创建 handler，启动监控。
5. 网络断开时 → 主程序调用当前 handler 的 `check_online()` → 返回 False → 调用 `login()` → 根据结果显示通知。

---

## 七、优势总结

- **高内聚低耦合**：主程序与协议完全解耦。
- **热插拔**：新增协议只需复制一个 `.py` 文件，无需修改主程序。
- **用户友好**：树形导航清晰，配置实时保存。
- **向后兼容**：自动迁移旧配置，原有功能不受影响。
